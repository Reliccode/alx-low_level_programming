#include<stdio.h>

/**
 * main-Entry point
 * The program uses printf to print
 * Return: Always 0 (success)
 */

int main(void)
{
	/*printing using printf*/
	char Exctly[] = "with proper grammar, but the outcome is a piece of art,";

	printf(Exctly \n); /*Writing the string to the stdout*/

	return (0);
}
